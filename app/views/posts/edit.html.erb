<% content_for :title, "commit rebase" %>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-lg-6">
      <h1 class="text-center display-5 fw-bold mb-4 text-primary">commit rebase</h1>
      
      <div class="card shadow-sm bg-white text-dark">
        <div class="card-body p-4">
          <%= render "form", post: @post %>
          
          <hr class="my-3">
          <div class="d-flex justify-content-center gap-2">
            <%= link_to "commit詳細へ", @post, class: "btn btn-secondary me-2" %>
            <%= link_to "commit logへ", posts_path, class: "btn btn-secondary" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
