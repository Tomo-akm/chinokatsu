<div id="<%= dom_id post %>" class="card shadow-sm bg-white text-dark">
  <div class="card-body">
    <div class="d-flex justify-content-between align-items-center mb-2">
      <span class="text-muted small"><i class="bi bi-person"></i> <%= post.user.name %></span>
      <span class="text-muted small"><i class="bi bi-clock"></i> <%= post.created_at.strftime("%Y/%m/%d %H:%M") %></span>
    </div>
    <div class="card-text fs-6 mb-3">
      <%= simple_format(h(post.content)) %>
    </div>
    <hr class="my-3">
    <div class="d-flex flex-wrap gap-2 justify-content-end">
      <%= link_to "詳細", post, class: "btn btn-outline-primary btn-sm" %>
      <% if user_signed_in? && post.user == current_user %>
        <%= link_to "編集", edit_post_path(post), class: "btn btn-outline-secondary btn-sm" %>
        <%= button_to "削除", post, method: :delete, class: "btn btn-outline-danger btn-sm", data: { confirm: "本当に削除しますか？" } %>
      <% end %>
    </div>
  </div>
</div>
