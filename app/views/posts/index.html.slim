- content_for :title, "ちのgit"

.container
  .row
    / サイドバー
    .col-lg-3
      .card
        .card-header
          h5.mb-0
            i.fas.fa-tags
            |  人気タグ
        .card-body
          - if @popular_tags.any?
            - @popular_tags.each do |tag|
              .d-flex.justify-content-between.align-items-center.mb-2
                = link_to tag_path(tag.name), class: "text-decoration-none" do
                  span.badge.bg-secondary.me-2 ##{tag.name}
                span.text-muted.small = tag.posts_count
            hr
            = link_to "すべてのタグを見る", tags_path, class: "btn btn-sm btn-outline-primary w-100"
          - else
            p.text-muted.mb-0 まだタグがありません

    / メインコンテンツ
    .col-lg-9
      .text-center.mb-4
        h1.display-4.fw-bold.text-primary ちのgit
        p.lead.text-muted.mt-3
          | 毎日コミットして、理想のmain branchに向かおう🚀
          br
          | みんなの就活進捗をcommit logで確認しよう！

      .text-center.mb-4
        = link_to "new commit", new_post_path, class: "btn btn-primary"

      / 検索フォーム（Ransack）
      = search_form_for @q, local: true, class: "mb-4" do |f|
        .input-group
          = f.search_field :content_cont, placeholder: "投稿内容で検索...", class: "form-control"
          button.btn.btn-outline-secondary type="submit"
            i.fas.fa-search
            |  検索

      .d-flex.flex-column.gap-3#posts
        - @posts.each do |post|
          = render post

      .d-flex.justify-content-center.mt-4
        = paginate @posts